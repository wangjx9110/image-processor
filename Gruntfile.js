module.exports = function(grunt) {
	grunt.initConfig({
		concat: {

		},
		uglify: {

		},
		htmlmin: {

		},
		cssmin: {

		},
		watch: {
			
		}
	});
	grunt.loadNpmTask();
	grunt.registerTask('default', []);
}